#include <reg51.h>

void main()
{
    unsigned int num1, num2;
    unsigned long product;

    num1 = 0x2222;
    num2 = 0xBBBB;

    while(1)
    {
        product = (unsigned long)num1 * num2;

        P0 = product & 0xFF;
        P1 = (product & 0xFF00) >> 8;
        P2 = (product & 0xFF0000) >> 16;
        P3 = (product & 0xFF000000) >> 24;
    }
}
